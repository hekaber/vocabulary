import { IoMenuOutline } from "react-icons/io5";
import {ActionButton} from "./styles";

export const MenuButton = () => {

    return (
        <ActionButton>
            <IoMenuOutline />
        </ActionButton>
    );
}